import { AddStuff } from "../components/AddStuff"
import { Navbar } from "../components/Navbar"

export const NotSoBigStuff = () => {
    return (
        <div>
            <Navbar />
            placeholder not so big stuff
            <AddStuff />
        </div>
    )
}